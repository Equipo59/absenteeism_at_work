# FastAPI and server (essential)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
python-multipart>=0.0.6

# Data processing (minimal, for model inference only)
numpy>=1.21.0
pandas>=1.3.0
scikit-learn>=1.0.0

# Model persistence
joblib>=1.3.0

# MLflow (for experiment tracking - optional but lightweight)
mlflow>=2.0.0
